- [x] F1 Backend CSV generation smoke test (`make smoke-test`) — VERIFIED
- [ ] F2 Planner service health endpoint (`curl -s -o "%{http_code}" http://localhost:8001/health`) — UNVERIFIED
- [ ] F3 Sandbox service health endpoint (`curl -s -o "%{http_code}" http://localhost:8002/health`) — UNVERIFIED
